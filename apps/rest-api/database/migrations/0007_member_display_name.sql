-- 우리가 붙이는 이름.
--
-- 사람이 고치기 전까지는 비워 둔다. 동기화가 CoC 이름으로 채우면, 나중에
-- 그 값이 사람이 정한 표기인지 동기화가 써 넣은 것인지 구분할 수 없다.
--
--   display_name  사람이 정한 표기. 아무도 고치지 않았으면 NULL
--   name          CoC 가 지금 들고 있는 이름. 동기화가 늘 덮는다
--
-- 비었을 때 무엇을 보여줄지는 화면이 정한다. 서버는 "사람이 정한 표기가 없다"는
-- 사실을 그대로 내보낸다.
ALTER TABLE clan_members ADD COLUMN display_name TEXT;
